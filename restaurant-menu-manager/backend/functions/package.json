{
  "name": "restaurant-menu-manager",
  "version": "1.0.0",
  "description": "Backend for managing restaurant menu items using Firebase.",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^10.0.0",
    "firebase-functions": "^3.14.1"
  },
  "devDependencies": {
    "typescript": "^4.5.4"
  },
  "engines": {
    "node": "14"
  }
}